#include <iostream>
#include <bits/stdc++.h>
 
using namespace std;
 
int main() {
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    double R; cin >> R;
    cout << setprecision(11) << 2 * acos(0.0) * R * R << endl;   
    return 0;
}